import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        margin: 16,
    },
    logo: {
        alignItems: 'center',
        marginVertical: 80,
    },
    inputContainer: {
        gap: 10,
        marginBottom: 30,
    },
});
